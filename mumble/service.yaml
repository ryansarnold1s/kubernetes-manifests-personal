apiVersion: v1
kind: Service
metadata:
  name: mumble
  namespace: mumble
  labels:
    app: mumble
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.130.154
  ports:
  - name: mumble-tcp
    port: 64738
    targetPort: 64738
    protocol: TCP
  - name: mumble-udp
    port: 64738
    targetPort: 64738
    protocol: UDP
  selector:
    app: mumble
